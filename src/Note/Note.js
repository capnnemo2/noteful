import React from 'react';
import dummyStore from '../dummyStore';

export default function Note(props) {
    // const note = dummyStore.find(n => n.id === props.match.params.noteId)
    return (
        <div className='Note'>
            {/* <h2>{note.name}</h2>
            <p>{note.content}</p> */}
        </div>
    )
}

